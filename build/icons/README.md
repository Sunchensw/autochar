MVP uses Electron's default icon.

To replace the Windows icon later, place an `.ico` file in `build/icons/` and add the `icon` field to the Electron builder config:

- `apps/recorder/electron-builder.yml`
